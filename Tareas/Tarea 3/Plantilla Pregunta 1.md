{
 "cells": [
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "# Pregunta 1 \n"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "import random\n",
    "from random import expovariate\n",
    "from collections import deque\n",
    "\n",
    "INF = float(\"inf\")   # Representa \"este evento no ocurrirá / no está programado\"\n"
   ]
  },
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "---\n",
    "## Parte (a) \n",
    "\n",
    "> **Instrucciones:** completa los bloques marcados con `# ▶▶▶ TODO` y `# ◀◀◀`.\n",
    "> Los métodos no marcados ya están implementados: léelos con atención antes de completar.\n"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "# ══════════════════════════════════════════════════════════════════════════════\n",
    "# CLASES DE ENTIDADES\n",
    "# ══════════════════════════════════════════════════════════════════════════════\n",
    "\n",
    "class Bus:\n",
    "    def __init__(self, t_llegada):\n",
    "        self.t_llegada = t_llegada   # Instante en que llega al sistema\n",
    "        self.t_frente  = None        # Instante en que pasa al frente del paradero\n",
    "        self.t_salida  = None        # Instante en que parte\n",
    "        self.pasajeros = []          # Pasajeros que viajan en él\n",
    "\n",
    "\n",
    "class Pasajero:\n",
    "    def __init__(self, t_paradero):\n",
    "        self.t_paradero      = t_paradero  # Sale de la fiesta y llega a la cola\n",
    "        self.t_inicio_subida = None        # Instante en que EMPIEZA a subir al bus\n",
    "        self.t_subida        = None        # Instante en que TERMINA de subir\n",
    "        self.t_salida        = None        # Instante en que parte el bus que lo lleva\n",
    "\n",
    "\n",
    "# ══════════════════════════════════════════════════════════════════════════════\n",
    "# SIMULACIÓN\n",
    "# ══════════════════════════════════════════════════════════════════════════════\n",
    "\n",
    "class Simulacion:\n",
    "\n",
    "    def __init__(self, beta, gamma, lambd, alpha, N, K):\n",
    "        self.beta  = beta    # Tasa llegada buses [buses/h]\n",
    "        self.gamma = gamma   # Tasa aburrimiento chofer [1/h]\n",
    "        self.lambd = lambd   # Tasa salida de CADA persona de la fiesta [1/h]\n",
    "        self.alpha = alpha   # Tasa de embarque de un pasajero [1/h]\n",
    "        self.N = N\n",
    "        self.K = K\n",
    "\n",
    "        self.t         = 0.0\n",
    "        self.en_fiesta = N\n",
    "        self.subidos   = 0    # Personas que ya partieron en algún bus\n",
    "\n",
    "        self.cola_personas = deque()  # Cola FIFO: personas esperando en el paradero\n",
    "        self.cola_buses    = deque()  # Cola FIFO: buses esperando turno al frente\n",
    "        self.bus = None               # Bus actualmente al frente (None si no hay)\n",
    "\n",
    "        # El inicio de la subida en curso se consolida en el pasajero\n",
    "        # solo si la subida termina con éxito (el bus no parte antes)\n",
    "        self._inicio_subida_actual = None\n",
    "\n",
    "        self.todos_pasajeros = []\n",
    "        self.todos_buses     = []\n",
    "\n",
    "        # Acumuladores para el promedio de largo de cola:\n",
    "        # largo_promedio = integral(largo(t) dt) / T  ≈  suma(largo_actual * dt) / T\n",
    "        self.area_cola_personas = 0.0\n",
    "        self.area_cola_buses    = 0.0\n",
    "\n",
    "        # Tiempos de los próximos eventos; INF = \"no está programado ahora\"\n",
    "        self.t_next_pasajero = self._sample_llegada_persona()\n",
    "        self.t_next_bus      = self.t + expovariate(self.beta)\n",
    "        self.t_next_subida   = INF\n",
    "        self.t_next_salida   = INF\n",
    "\n",
    "    # ── Métodos auxiliares ───────────────────────────────────────────────────\n",
    "\n",
    "    def _sample_llegada_persona(self):\n",
    "        \"\"\"Retorna el instante del próximo evento 'persona sale de la fiesta'.\"\"\"\n",
    "        if self.en_fiesta <= 0:\n",
    "            return INF\n",
    "        \n",
    "        # ▶▶▶ TODO\n",
    "        pass\n",
    "        # ◀◀◀\n",
    "        return ##Completar esta parte\n",
    "\n",
    "    def _programar_subida(self):\n",
    "        \"\"\"\n",
    "        Programa el próximo evento 'fin de subida' si se cumplen TODAS estas condiciones:\n",
    "          - Hay un bus al frente (self.bus no es None).\n",
    "          - El bus no está lleno (len < K).\n",
    "          - Hay al menos una persona esperando en cola_personas.\n",
    "        Si se cumplen: registra self._inicio_subida_actual = self.t y samplea t_next_subida.\n",
    "        Si no se cumplen: cancela el evento poniendo _inicio_subida_actual = None\n",
    "          y t_next_subida = INF.\n",
    "        \"\"\"\n",
    "        if (self.bus is not None\n",
    "                and len(self.bus.pasajeros) < self.K\n",
    "                and len(self.cola_personas) > 0):\n",
    "            self._inicio_subida_actual = self.t\n",
    "            self.t_next_subida = self.t + expovariate(self.alpha)\n",
    "        else:\n",
    "            self._inicio_subida_actual = None\n",
    "            self.t_next_subida = INF\n",
    "\n",
    "    def _bus_al_frente(self, bus):\n",
    "        \"\"\"\n",
    "        Coloca un bus en la posición activa (frente al paradero).\n",
    "        El chofer empieza a contar su paciencia Exp(gamma) desde este instante.\n",
    "        Actualiza: self.bus, bus.t_frente, self.t_next_salida.\n",
    "        Luego llama a _programar_subida para ver si hay pasajeros esperando.\n",
    "        \"\"\"\n",
    "        self.bus           = bus\n",
    "        bus.t_frente       = self.t\n",
    "        self.t_next_salida = self.t + expovariate(self.gamma)\n",
    "        self._programar_subida()\n",
    "\n",
    "    def _parte_bus(self):\n",
    "        \"\"\"\n",
    "        Despacha el bus al frente (ya sea por estar lleno o por aburrimiento del chofer).\n",
    "\n",
    "        Debes:\n",
    "          1. Registrar bus.t_salida = self.t y p.t_salida = self.t para cada pasajero a bordo.\n",
    "          2. Sumar len(bus.pasajeros) a self.subidos.\n",
    "          3. Limpiar el estado: self.bus = None, _inicio_subida_actual = None,\n",
    "             t_next_salida = INF, t_next_subida = INF.\n",
    "          4. Si hay buses en cola_buses, avanzar el siguiente llamando a _bus_al_frente.\n",
    "        \"\"\"\n",
    "        # ▶▶▶ TODO\n",
    "        pass\n",
    "        # ◀◀◀\n",
    "\n",
    "    # ── Bucle principal ──────────────────────────────────────────────────────\n",
    "\n",
    "    def simulate(self):\n",
    "\n",
    "        while self.subidos < self.N:\n",
    "\n",
    "            # 1. Próximo evento\n",
    "            t_next = min(self.t_next_pasajero, self.t_next_bus,\n",
    "                         self.t_next_subida,   self.t_next_salida)\n",
    "\n",
    "            # 2. Acumular área (largo actual × Δt)\n",
    "            dt = t_next - self.t\n",
    "            self.area_cola_personas += len(self.cola_personas) * dt\n",
    "            self.area_cola_buses    += len(self.cola_buses)    * dt\n",
    "\n",
    "            # 3. Avanzar reloj\n",
    "            self.t = t_next\n",
    "\n",
    "            # 4. Procesar evento\n",
    "            if t_next == self.t_next_pasajero:\n",
    "                # EVENTO: Llegada de persona al paradero\n",
    "                p = Pasajero(self.t)\n",
    "                self.todos_pasajeros.append(p)\n",
    "                self.cola_personas.append(p)\n",
    "                self.en_fiesta -= 1\n",
    "                self.t_next_pasajero = self._sample_llegada_persona()\n",
    "                if self.t_next_subida == INF:\n",
    "                    self._programar_subida()\n",
    "\n",
    "            elif t_next == self.t_next_bus:\n",
    "                # EVENTO: Llegada de bus\n",
    "                nuevo = Bus(self.t)\n",
    "                self.todos_buses.append(nuevo)\n",
    "                if self.bus is None:\n",
    "                    self._bus_al_frente(nuevo)    # Pasa directo al frente\n",
    "                else:\n",
    "                    self.cola_buses.append(nuevo) # Se pone en la fila\n",
    "                self.t_next_bus = self.t + expovariate(self.beta)\n",
    "\n",
    "            elif t_next == self.t_next_subida:\n",
    "                # EVENTO: Fin de subida de un pasajero\n",
    "                p = self.cola_personas.popleft()\n",
    "                p.t_inicio_subida = self._inicio_subida_actual\n",
    "                p.t_subida        = self.t\n",
    "                self.bus.pasajeros.append(p)\n",
    "                if len(self.bus.pasajeros) >= self.K:\n",
    "                    self._parte_bus()\n",
    "                else:\n",
    "                    self._programar_subida()\n",
    "\n",
    "            elif t_next == self.t_next_salida:\n",
    "                # EVENTO: Chofer se aburre y parte\n",
    "                self._parte_bus()\n",
    "\n",
    "        # ════════════════════════════════════════════════════════════════════\n",
    "        # ESTADÍSTICOS\n",
    "        # ════════════════════════════════════════════════════════════════════\n",
    "        T = self.t\n",
    "\n",
    "        # (ii) Largo promedio de la cola de personas\n",
    "        # Pista: usa el área acumulada en self.area_cola_personas.\n",
    "        # ▶▶▶ TODO\n",
    "        prom_cola_personas = None\n",
    "        # ◀◀◀\n",
    "\n",
    "        # (iii) Largo promedio de la cola de buses\n",
    "        # ▶▶▶ TODO\n",
    "        prom_cola_buses = None\n",
    "        # ◀◀◀\n",
    "\n",
    "        # (iv) Tiempo promedio desde que sale de la fiesta hasta que parte el bus\n",
    "        # Pista: para cada pasajero calcula (t_salida - t_paradero) y promedia.\n",
    "        # ▶▶▶ TODO\n",
    "        prom_espera_total = None\n",
    "        # ◀◀◀\n",
    "\n",
    "        # (v) Tiempo promedio de espera en cola\n",
    "        # La espera en cola va desde que llega al paradero hasta que EMPIEZA a subir\n",
    "        # (no hasta que termina de subir: eso incluiría el tiempo de servicio).\n",
    "        # Filtra pasajeros con t_inicio_subida != None.\n",
    "        # ▶▶▶ TODO\n",
    "        prom_espera_cola = None\n",
    "        # ◀◀◀\n",
    "\n",
    "        # (vi) Promedio de personas por bus (solo buses que llevaron ≥ 1 pasajero)\n",
    "        # ▶▶▶ TODO\n",
    "        prom_pasajeros_bus  = None\n",
    "        buses_con_pasajeros = None\n",
    "        # ◀◀◀\n",
    "\n",
    "        return {\n",
    "            \"(i)   Tiempo total hasta vaciar el sistema (h)\":            T,\n",
    "            \"(ii)  Largo promedio cola personas\":                         prom_cola_personas,\n",
    "            \"(iii) Largo promedio cola buses\":                            prom_cola_buses,\n",
    "            \"(iv)  Tiempo promedio salida fiesta → partida del bus (h)\": prom_espera_total,\n",
    "            \"(v)   Tiempo promedio de espera en cola (h)\":               prom_espera_cola,\n",
    "            \"(vi)  Personas promedio por bus\":                            prom_pasajeros_bus,\n",
    "            \"      Buses con pasajeros\":                                  buses_con_pasajeros,\n",
    "        }\n",
    "\n",
    "\n",
    "# ── Ejecución ────────────────────────────────────────────────────────────────\n",
    "random.seed(2123)\n",
    "sim = Simulacion(beta=11, gamma=4, lambd=12, alpha=120, N=2000, K=30)\n",
    "resultados = sim.simulate()\n",
    "\n",
    "print(\"=== RESULTADOS PARTE (a) ===\")\n",
    "for k, v in resultados.items():\n",
    "    print(f\"  {k}: {v:.6f}\" if isinstance(v, float) else f\"  {k}: {v}\")\n"
   ]
  },
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "---\n",
    "## Parte (b) \n",
    "> **Instrucciones:** completa los bloques marcados con `# ▶▶▶ TODO` y `# ◀◀◀`.\n",
    "> Los métodos no marcados ya están implementados: léelos con atención antes de completar los tuyos."
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "# ══════════════════════════════════════════════════════════════════════════════\n",
    "# CLASES DE ENTIDADES\n",
    "# ══════════════════════════════════════════════════════════════════════════════\n",
    "\n",
    "class BusB:\n",
    "    def __init__(self, t_llegada, gamma, K):\n",
    "        self.t_llegada       = t_llegada\n",
    "        self.t_salida_chofer = t_llegada + expovariate(gamma)  # Paciencia desde que llega\n",
    "        self.t_fin_subida    = INF        # Tiempo en que termina la subida en curso\n",
    "        self.pasajeros       = []         # Pasajeros ya a bordo\n",
    "        self.cola            = deque()    # Personas esperando subir A ESTE bus\n",
    "        self.subiendo        = None       # Persona actualmente en la puerta\n",
    "        self.K               = K\n",
    "        self.t_salida        = None\n",
    "\n",
    "\n",
    "class PasajeroB:\n",
    "    def __init__(self, t_paradero):\n",
    "        self.t_paradero      = t_paradero\n",
    "        self.t_inicio_subida = None   # Cuando empieza a subir (para medir espera en cola)\n",
    "        self.t_subida        = None   # Cuando termina de subir\n",
    "        self.t_salida        = None   # Cuando parte el bus\n",
    "\n",
    "\n",
    "# ══════════════════════════════════════════════════════════════════════════════\n",
    "# SIMULACIÓN\n",
    "# ══════════════════════════════════════════════════════════════════════════════\n",
    "\n",
    "class SimulacionParteB:\n",
    "\n",
    "    def __init__(self, beta, gamma, lambd, alpha, N, K):\n",
    "        self.beta  = beta\n",
    "        self.gamma = gamma\n",
    "        self.lambd = lambd\n",
    "        self.alpha = alpha\n",
    "        self.N = N\n",
    "        self.K = K\n",
    "\n",
    "        self.t         = 0.0\n",
    "        self.en_fiesta = N\n",
    "        self.subidos   = 0\n",
    "\n",
    "        self.cola_global   = deque()  # Personas que llegaron sin buses disponibles\n",
    "        self.buses_activos = []       # Buses actualmente abiertos\n",
    "\n",
    "        self.todos_pasajeros = []\n",
    "        self.todos_buses     = []\n",
    "\n",
    "        self.area_cola_personas = 0.0\n",
    "        self.area_cola_buses    = 0.0\n",
    "\n",
    "        self.t_next_pasajero = self._sample_llegada_persona()\n",
    "        self.t_next_bus      = expovariate(self.beta)\n",
    "\n",
    "    # ── Métodos auxiliares ───────────────────────────────────────────────────\n",
    "\n",
    "    def _sample_llegada_persona(self):\n",
    "        if self.en_fiesta <= 0:\n",
    "            return INF\n",
    "        \n",
    "        # ▶▶▶ TODO\n",
    "        pass\n",
    "        # ◀◀◀\n",
    "        return ##Completar esta parte\n",
    "\n",
    "    def _iniciar_subida(self, b, p):\n",
    "        \"\"\"\n",
    "        Registra que la persona p empieza a subir al bus b en este instante.\n",
    "        Actualiza: b.subiendo = p, p.t_inicio_subida = self.t,\n",
    "        y samplea b.t_fin_subida ~ Exp(alpha).\n",
    "        \"\"\"\n",
    "        b.subiendo        = p\n",
    "        p.t_inicio_subida = self.t\n",
    "        b.t_fin_subida    = self.t + expovariate(self.alpha)\n",
    "\n",
    "    def _asignar_personas(self, personas):\n",
    "        \"\"\"\n",
    "        Asigna cada persona de la lista a un bus activo o a la cola global.\n",
    "\n",
    "        Para cada persona:\n",
    "          - Si no hay buses activos → agregar a self.cola_global.\n",
    "          - Si hay buses activos → elegir uno al azar con random.choice(self.buses_activos),\n",
    "            agregar la persona a b.cola.\n",
    "            Si ese bus estaba ocioso (b.subiendo == None), sacar a la persona de b.cola\n",
    "            e iniciar su subida llamando a _iniciar_subida(b, persona).\n",
    "\n",
    "        Debes iterar sobre todas las personas de la lista.\n",
    "        \"\"\"\n",
    "        # ▶▶▶ TODO\n",
    "        pass\n",
    "        # ◀◀◀\n",
    "\n",
    "    def _parte_bus(self, b):\n",
    "        \"\"\"\n",
    "        Despacha el bus b.\n",
    "\n",
    "        Debes:\n",
    "          1. Registrar b.t_salida = self.t y p.t_salida = self.t para cada pasajero a bordo.\n",
    "             Sumar len(b.pasajeros) a self.subidos.\n",
    "          2. Construir lista de 'varados': si b.subiendo != None, esa persona NO logró subir;\n",
    "             resetear su t_inicio_subida = None y agregarla a varados. Luego agregar\n",
    "             todos los de b.cola a varados.\n",
    "          3. Remover b de self.buses_activos y agregarlo a self.todos_buses.\n",
    "          4. Reasignar los varados llamando a self._asignar_personas(varados).\n",
    "        \"\"\"\n",
    "        # ▶▶▶ TODO\n",
    "        pass\n",
    "        # ◀◀◀\n",
    "\n",
    "    # ── Bucle principal ──────────────────────────────────────────────────────\n",
    "\n",
    "    def simulate(self):\n",
    "\n",
    "        while self.subidos < self.N:\n",
    "\n",
    "            # 1. Buscar el próximo evento entre TODOS los buses activos\n",
    "            t_fin_subida_min = INF;  bus_fin_subida = None\n",
    "            t_salida_min     = INF;  bus_salida     = None\n",
    "\n",
    "            for b in self.buses_activos:\n",
    "                if b.t_fin_subida    < t_fin_subida_min:\n",
    "                    t_fin_subida_min  = b.t_fin_subida;    bus_fin_subida = b\n",
    "                if b.t_salida_chofer < t_salida_min:\n",
    "                    t_salida_min      = b.t_salida_chofer; bus_salida     = b\n",
    "\n",
    "            t_next = min(self.t_next_pasajero, self.t_next_bus,\n",
    "                         t_fin_subida_min, t_salida_min)\n",
    "\n",
    "            # 2. Acumular áreas\n",
    "            dt = t_next - self.t\n",
    "            largo_cola = len(self.cola_global) + sum(len(b.cola) for b in self.buses_activos)\n",
    "            self.area_cola_personas += largo_cola * dt\n",
    "            self.area_cola_buses    += len(self.buses_activos) * dt\n",
    "\n",
    "            # 3. Avanzar reloj\n",
    "            self.t = t_next\n",
    "\n",
    "            # 4. Procesar evento\n",
    "            if t_next == self.t_next_pasajero:\n",
    "                # EVENTO: Llegada de persona al paradero\n",
    "                p = PasajeroB(self.t)\n",
    "                self.todos_pasajeros.append(p)\n",
    "                self.en_fiesta -= 1\n",
    "                self._asignar_personas([p])\n",
    "                self.t_next_pasajero = self._sample_llegada_persona()\n",
    "\n",
    "            elif t_next == self.t_next_bus:\n",
    "                # EVENTO: Llegada de bus\n",
    "                nuevo_bus = BusB(self.t, self.gamma, self.K)\n",
    "                self.buses_activos.append(nuevo_bus)\n",
    "                if self.cola_global:\n",
    "                    gente = list(self.cola_global)\n",
    "                    self.cola_global.clear()\n",
    "                    self._asignar_personas(gente)\n",
    "                self.t_next_bus = self.t + expovariate(self.beta)\n",
    "\n",
    "            elif t_next == t_fin_subida_min:\n",
    "                # EVENTO: Fin de subida en algún bus\n",
    "                b = bus_fin_subida\n",
    "                p = b.subiendo\n",
    "                p.t_subida = self.t\n",
    "                b.pasajeros.append(p)\n",
    "                b.subiendo     = None\n",
    "                b.t_fin_subida = INF\n",
    "                if len(b.pasajeros) >= self.K:\n",
    "                    self._parte_bus(b)\n",
    "                elif b.cola:\n",
    "                    self._iniciar_subida(b, b.cola.popleft())\n",
    "\n",
    "            elif t_next == t_salida_min:\n",
    "                # EVENTO: Chofer se aburre y parte\n",
    "                self._parte_bus(bus_salida)\n",
    "\n",
    "        # ════════════════════════════════════════════════════════════════════\n",
    "        # ESTADÍSTICOS\n",
    "        # ════════════════════════════════════════════════════════════════════\n",
    "        T = self.t\n",
    "\n",
    "        # (ii) Largo promedio de la cola de personas\n",
    "        # La cola incluye cola_global + colas individuales de cada bus.\n",
    "        # Pista: ya se acumuló correctamente en self.area_cola_personas.\n",
    "        # ▶▶▶ TODO\n",
    "        prom_cola_personas = None\n",
    "        # ◀◀◀\n",
    "\n",
    "        # (iii) Promedio de buses abiertos simultáneamente\n",
    "        # ▶▶▶ TODO\n",
    "        prom_cola_buses = None\n",
    "        # ◀◀◀\n",
    "\n",
    "        # (iv) Tiempo promedio desde salida de fiesta hasta partida del bus\n",
    "        # Filtra pasajeros con t_salida != None.\n",
    "        # ▶▶▶ TODO\n",
    "        prom_espera_total = None\n",
    "        # ◀◀◀\n",
    "\n",
    "        # (v) Tiempo promedio de espera en cola (hasta que EMPIEZA a subir)\n",
    "        # ▶▶▶ TODO\n",
    "        prom_espera_cola = None\n",
    "        # ◀◀◀\n",
    "\n",
    "        # (vi) Promedio de personas por bus (solo buses con ≥ 1 pasajero)\n",
    "        # ▶▶▶ TODO\n",
    "        prom_pasajeros_bus  = None\n",
    "        buses_con_pasajeros = None\n",
    "        # ◀◀◀\n",
    "\n",
    "        return {\n",
    "            \"(i)   Tiempo total hasta vaciar el sistema (h)\":            T,\n",
    "            \"(ii)  Largo promedio cola personas\":                         prom_cola_personas,\n",
    "            \"(iii) Promedio de buses abiertos simultáneamente\":          prom_cola_buses,\n",
    "            \"(iv)  Tiempo promedio salida fiesta → partida del bus (h)\": prom_espera_total,\n",
    "            \"(v)   Tiempo promedio de espera en cola (h)\":               prom_espera_cola,\n",
    "            \"(vi)  Personas promedio por bus\":                            prom_pasajeros_bus,\n",
    "            \"      Buses con pasajeros\":                                  buses_con_pasajeros,\n",
    "        }\n",
    "\n",
    "\n",
    "# ── Ejecución ────────────────────────────────────────────────────────────────\n",
    "random.seed(2123)\n",
    "sim = SimulacionParteB(beta=11, gamma=4, lambd=12, alpha=120, N=2000, K=30)\n",
    "resultados = sim.simulate()\n",
    "\n",
    "print(\"=== RESULTADOS PARTE (b) ===\")\n",
    "for k, v in resultados.items():\n",
    "    print(f\"  {k}: {v:.6f}\" if isinstance(v, float) else f\"  {k}: {v}\")\n"
   ]
  }
 ],
 "metadata": {
  "kernelspec": {
   "display_name": "Python 3",
   "language": "python",
   "name": "python3"
  },
  "language_info": {
   "name": "python",
   "version": "3.10.0"
  }
 },
 "nbformat": 4,
 "nbformat_minor": 5
}
